{% extends 'base.html.twig' %}

{% block title %}Edit InventoryLine{% endblock %}

{% block body %}
	<h1>Edit InventoryLine</h1>

	{{ include('award/_form.html.twig') }}

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('award_index', {'member': award.member.id}) }}" class="btn btn-light"><i class="fas fa-list" title="{{ 'global.action.list'|trans }}" aria-hidden="true"></i><span class="sr-only">{{ 'global.action.list'|trans }}</span></a>
		</li>
		<li class="list-inline-item">
			{{ include('award/_delete_form.html.twig') }}
		</li>
	</ul>
{% endblock %}
