{% extends 'base.html.twig' %}

{% block title %}Level | {{ parent() }}{% endblock %}

{% block body %}
	<h1>Level</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ level.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ level.name }}</td>
			</tr>
		</tbody>
	</table>

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('level_index') }}" class="btn btn-light"><i class="fas fa-list" title="{{ 'global.action.list'|trans }}" aria-hidden="true"></i><span class="visually-hidden">{{ 'global.action.list'|trans }}</span></a>
		</li>
		<li class="list-inline-item">
			<a href="{{ path('level_edit', {'id': level.id}) }}" class="btn btn-warning"><i class="fas fa-pencil-alt" title="{{ 'global.action.edit'|trans }}" aria-hidden="true"></i><span class="visually-hidden">{{ 'global.action.edit'|trans }}</span></a>
		</li>
		<li class="list-inline-item">
			{{ include('level/_delete_form.html.twig') }}
		</li>
	</ul>
{% endblock %}
