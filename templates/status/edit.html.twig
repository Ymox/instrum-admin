{% extends 'base.html.twig' %}

{% block title %}{{ status }} | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ status }}</h1>

	{{ include('status/_form.html.twig') }}

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('status_index') }}" class="btn btn-light"><i class="fas fa-list" title="{{ 'global.action.list'|trans }}" aria-hidden="true"></i><span class="visually-hidden">{{ 'global.action.list'|trans }}</span></a>
		</li>
		<li class="list-inline-item">
			{{ include('status/_delete_form.html.twig') }}
		</li>
	</ul>
{% endblock %}
