{% extends 'base.html.twig' %}

{% block title %}Edit Subcategory | {{ parent() }}{% endblock %}

{% block body %}
	<h1>Edit Subcategory</h1>

	{{ include('subcategory/_form.html.twig') }}

	<ul class="list-inline float-end">
		{#<li class="list-inline-item">
			<a href="{{ path('subcategory_index') }}" class="btn btn-light"><i class="fas fa-list" title="{{ 'global.action.list'|trans }}" aria-hidden="true"></i><span class="sr-only">{{ 'global.action.list'|trans }}</span></a>
		</li>#}
		<li class="list-inline-item">
			{{ include('subcategory/_delete_form.html.twig') }}
		</li>
	</ul>
{% endblock %}
