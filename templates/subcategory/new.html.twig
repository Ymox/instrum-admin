{% extends 'base.html.twig' %}

{% block title %}New Subcategory | {{ parent() }}{% endblock %}

{% block body %}
	<h1>{{ 'app.Create.new.heading'|trans }} Subcategory</h1>

	{{ include('subcategory/_form.html.twig') }}

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('subcategory_index') }}" class="btn btn-light"><i class="fas fa-list" title="{{ 'global.action.list'|trans }}" aria-hidden="true"></i><span class="visually-hidden">{{ 'global.action.list'|trans }}</span></a>
		</li>
	</ul>
{% endblock %}
