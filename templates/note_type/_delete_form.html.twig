<form method="post" action="{{ path('note_type_delete', {'id': noteType.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ noteType.id) }}">
	<button class="btn">Delete</button>
</form>
