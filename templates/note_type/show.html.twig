{% extends 'base.html.twig' %}

{% block title %}NoteType{% endblock %}

{% block body %}
	<h1>NoteType</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ note_type.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ note_type.name }}</td>
			</tr>
			<tr>
				<th>Description</th>
				<td>{{ note_type.description }}</td>
			</tr>
		</tbody>
	</table>

	<ul class="list-inline float-end">
		<li class="list-inline-item">
			<a href="{{ path('member_index') }}" class="btn btn-light"><i class="fas fa-list" title="{{ 'global.action.list'|trans }}" aria-hidden="true"></i><span class="sr-only">{{ 'global.action.list'|trans }}</span></a>
		</li>

	<a href="{{ path('note_type_edit', {'id': note_type.id}) }}">edit</a>

	{{ include('note_type/_delete_form.html.twig') }}
{% endblock %}
